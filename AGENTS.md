# AGENTS.md

本文件适用于整个 Dedicated to Fx 仓库。任何自动化代理或贡献者在修改项目前必须先阅读本文件，以及 docs 目录中与任务相关的产品文档。

## 1. 当前阶段门禁

- Fx 已于 2026-08-02 明确确认开始第一版开发，当前阶段进入 MVP 实现。
- 优先完成“文章阅读 → 表达探索 → 返回重读 → 本地记录 → 数据备份”的纵向闭环。
- 不得因进入开发阶段而扩张到登录、云同步、自动翻译或远程 AI 等非目标。

## 2. 产品定位

Dedicated to Fx 是为 Fx 个人长期使用的英语阅读学习系统，并包含只属于 Fx 的日记与计划空间。

它以完整英文文章为入口，通过真实语境、渐进提示、核心概念连接、迁移练习和重新阅读，帮助用户从依赖中文翻译逐渐转向直接感知英文表达。

它不是：

- 英中词典。
- 单词打卡或背词列表。
- 自动翻译阅读器。
- 刷题、排名或商业教育平台。

任何功能如果提高了“查到答案”的速度，却削弱了语境推断、文章连续性或迁移理解，都不应默认加入。

## 3. 学习原则

以下规则不可违反，优先级由上到下：

1. **文章优先**：学习从完整文章开始，不能把孤立词表作为默认入口。
2. **阅读优先**：正文保持干净，只探索用户主动选择或真正影响理解的表达。
3. **猜测先于解释**：点击表达后先邀请用户猜测，不能立即显示中文或答案。
4. **渐进提示顺序固定**：
   - Guess
   - Context clues
   - Part of speech & collocations
   - Simple English meaning
   - Contextual meaning
   - Core concept
   - Related contexts
   - Transfer practice
5. **当前语境与核心概念分离**：前者解释作者此处的表达，后者连接多个语境。
6. **禁止 word = 中文**：中文只作为后置的语境校准脚手架。
7. **表达不等于单词**：数据和 UI 必须支持短语、搭配和结构。
8. **探索后回到原文**：恢复位置并邀请重读，不能默认跳到下一张词卡。
9. **掌握需要证据**：查看答案、点击或收藏都不能自动标记掌握；迁移状态需要延迟的新语境证据。
10. **保留学习过程**：猜测、错误、修订、提示深度和原语境不得被最终答案覆盖。

学习逻辑的详细依据见 docs/LEARNING_MODEL.md。

## 4. 技术规则

### 固定技术路线

- React。
- TypeScript。
- Vite。
- PWA。
- IndexedDB。
- Dexie。
- GitHub Pages。
- 移动端优先，必须支持 iPhone Safari 与添加到主屏幕。

未经 Fx 明确授权，不引入：

- 云服务器或后端数据库。
- 登录和账号系统。
- 需要客户端 API Key 的远程服务。
- 远程分析、广告或用户追踪 SDK。

### 本地优先

- 文章内容和学习记录的核心流程不依赖在线 API。
- 已下载文章必须可离线阅读。
- 用户数据存储在 IndexedDB，并提供版本化 JSON 导出与恢复。
- Service Worker 缓存不是用户数据存储，不得把缓存清理等同于删除学习记录。
- 对 iOS 可能清理站点数据的风险保持诚实，并提供备份提醒。

### 数据访问

- UI 组件不得直接操作 Dexie 表。
- 通过 src/db 的 repository/service 边界读写数据。
- 领域类型放在 src/domain；持久化记录和 UI view model 不混为同一类型。
- 跨表学习写入使用事务。
- 每次 schema 修改都增加显式版本迁移和测试。
- 禁止用删除数据库或清空表来“修复”生产迁移。
- 历史 Encounter 保存上下文快照；内容更新不得静默改写历史证据。
- 导入数据是不可信输入，写入前必须完整校验。

### 路由与部署

- 路由必须兼容 GitHub Pages 子路径和页面刷新。
- Vite base 由部署路径显式配置，禁止假设站点部署在域名根目录。
- 浏览器返回必须能从 Exploration 回到原文章和原位置。

## 5. 目录约定

预期结构：

~~~
docs/                       产品与工程决策文档
public/
  content/                  经许可的静态文章内容包
  icons/                    PWA 图标
src/
  app/                      应用入口、路由、Provider、错误边界
  components/
    ui/                     无业务语义的基础组件
  content/                  内容解析、校验、安装
  db/                       Dexie schema、迁移、repository
  domain/                   领域类型与纯规则
  features/
    dashboard/
    library/
    reader/
    exploration/
    journal/
    history/
    planning/
    review/
    settings/
  pwa/                      安装、更新、离线状态
  styles/                   tokens、global、排版
  test/                     测试工具与 fixtures
~~~

优先按 feature 聚合业务 UI、hooks 和测试。只有真正跨 feature 且无业务语义的组件才放入 components/ui。

## 6. 代码规范

### TypeScript

- 开启 strict，不使用隐式 any。
- 禁止用 any 绕过领域建模；外部输入先作为 unknown，再经过校验收窄。
- 领域枚举优先使用字符串联合或只读常量，保证导出 JSON 可读。
- 时间持久化为 UTC ISO 字符串，显示时再本地化。
- ID、schemaVersion、contentVersion 的语义必须显式。

### React

- 使用函数组件和 hooks。
- 状态尽量靠近使用位置；没有清晰跨页面需求时不增加全局状态库。
- 服务端/数据库派生数据与临时表单状态分开。
- 副作用放在 hooks 或 service 边界，纯展示组件不直接读写存储。
- 不为每个正文 token 建立全局订阅；长文交互需关注渲染成本。
- 草稿和关键学习步骤必须持久化，不能只依赖组件内存。

### 命名与文件

- React 组件：PascalCase.tsx。
- hooks：useSomething.ts。
- 普通模块：kebab-case.ts。
- 测试靠近被测模块，使用 .test.ts 或 .test.tsx。
- 领域命名统一使用 Article、ExpressionConcept、LearningUnit、Encounter、ExplorationSession、PracticeAttempt、LearningSummary。
- 不把 ExpressionConcept 命名为 Translation 或 VocabularyItem。

### 样式

- 颜色、字体、间距、圆角和动效时长使用 design tokens。
- 先使用语义化 HTML 和 CSS；避免为普通布局引入重量级组件库。
- 正文阅读宽度、字号和行高是首要视觉约束。
- 尊重 prefers-reduced-motion。
- 不用颜色作为唯一状态信号。

### 内容安全

- 文章以受控结构化文本渲染，不直接注入未验证 HTML。
- 禁止使用未经清理的 dangerouslySetInnerHTML。
- 所有文章和例句必须带来源/许可或明确标记为原创。
- 不直接复制受版权保护的长文或词典释义。

## 7. UX 与可访问性规则

- 移动端为首要设计视口，但桌面不能退化为被拉宽的手机页。
- 最小触控目标约 44×44 CSS px。
- 正文可缩放，不能禁止浏览器缩放。
- 折叠提示使用真实 button 和 aria-expanded。
- 面板/对话框打开后管理焦点，关闭后回到触发表达。
- 原生文本选择、复制和辅助技术不能被单词点击逻辑破坏。
- 加载、离线、空数据、存储失败和迁移失败都必须有明确状态。
- 动效服务于层级、定位和连续性，不做奖励刺激。

视觉基调是 ink、paper、memory、growth、knowledge、reflection：安静、克制、高级、有艺术感。禁止儿童化、卡通化、游戏化和商业课程平台风格。

## 8. 禁止事项

- 禁止首页以“待背单词数”为主任务。
- 禁止点击单词立刻弹出中文释义。
- 禁止自动高亮满篇“生词”。
- 禁止把多个中文义项冒充核心概念。
- 禁止一次答对或看过答案后显示“已掌握”。
- 禁止探索完成后默认进入下一个单词。
- 禁止金币、排行榜、惩罚性连胜、倒计时焦虑。
- 禁止将用户记录上传到远端。
- 禁止在源码、构建变量、浏览器存储中放置 API Key。
- 禁止无确认覆盖导入数据或执行不可恢复的清除。
- 禁止通过删除旧数据规避数据库迁移。
- 禁止为了扩展内容而使用版权不明的文章或例句。

## 9. 测试与完成定义

每个功能变更应按风险提供测试：

- 领域规则：单元测试。
- 交互与可访问性：组件测试。
- 阅读—探索—返回、离线、备份恢复：端到端测试。
- PWA 安装和 iOS 行为：真机检查。

功能只有在以下条件满足时才算完成：

- 符合 PRD 和学习模型。
- 移动端主路径可用，键盘与焦点行为正确。
- 刷新、离线或恢复后数据不丢失。
- 错误和空状态已处理。
- 测试通过，生产构建通过。
- 若更改行为、schema 或内容格式，相关 docs 同步更新。

## 10. 工作方式

- 开始任务前先检查工作区状态，保护用户已有修改。
- 只修改与当前任务有关的文件，不顺手重构无关代码。
- 优先做可验证的纵向切片，不一次铺开大量空页面。
- 重大新增依赖、数据破坏、产品原则变化或范围扩张必须先获得 Fx 确认。
- 对未确定的问题记录假设和取舍，不以“先写代码再说”代替产品决策。
- 提交交付时说明改了什么、如何验证、仍有哪些明确限制。
